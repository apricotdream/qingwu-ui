# 项目记忆 · qingwu-ui（青梧UI）

> 自动绑定的项目记忆文件（AGENTS.md §3 规范）。追加不覆盖、去重、重要条目标注日期。禁止存储密钥/密码/Token。

## 一、项目基础信息

- 定位：以中国历法（农历/节气/节假日/调休）为核心差异化、无障碍优先的框架无关日历/日期选择组件库。
- 仓库：qingwu-ui monorepo（bun workspaces + Turborepo，Node >= 20，packageManager bun@1.3.13）。
- 现状基线：处于 REFACTOR_PLAN.md（v1.0，2026-07-25）驱动的重构早期——`plugin/calendar` 下的旧 vendor 克隆已于 2026-07-28 删除。
- 2026-07-31：**`@qingwu/calendar-core` 并入 `@qingwu/calendar`**——core 仅有 44 行日期工具且 ui 只引用其 `DayMeta` 类型（且 dayRenderer/RichDayMeta/selectedDetail 均为死代码），判定独立包无必要，删除 `packages/calendar/core` 并清理死代码；headless 状态机将来作为 @qingwu/calendar 包内模块实现（不再独立成包）。

## 二、架构与目录约定

- 目标结构：`packages/calendar/{calendar,react,vue,compat}` + `packages/{lunar,icons}` + `apps/{docs,playground}` + `tooling/tsconfig`（详见 REFACTOR_PLAN.md §6.1；calendar-core 已于 2026-07-31 并入 calendar，不再独立成包）。
- 渲染层归属：原生 TS DOM 渲染为正统（@qingwu/calendar），React/Vue 仅 ~100 行生命周期薄包装（决策见 REFACTOR_PLAN.md §6.6）。
- 样式架构：`@layer qingwu.reset, qingwu.tokens, qingwu.base, qingwu.components, qingwu.theme`；tokens 单源（正式版由 tokens.json 生成）；用户主题写在 qingwu.theme 层即可覆盖。
- **demo/index.html 是当前唯一的活体皮肤层原型**：新 UI 组件先在此按 tokens + @layer 约定实现，后续提取进 @qingwu/calendar 的 `./css` 子路径导出。

## 三、已做决策与技术选型

- TS 7.0.2（native 编译器主线）+ tsdown 0.22 + vitest 4.x + Biome 2.x（lint+format 单工具）+ size-limit CI 卡口；Phase 0 设 TS7 降级 TS 5.x 的硬关卡。
- 农历数据基座为 `packages/calendar/ui/src/lunar.ts`（自研实现，零依赖），覆盖 1900-2100 年农历查表 + 节气年份修正。已移除 lunar-javascript / lunar-typescript 外部依赖。
- 体积预算：@qingwu/calendar ≤ 30KB；@qingwu/search ≤ 12KB；lunar 子路径 ≤ 30KB（规划中，未引入时 0 字节）。
- 2026-08-01：**日历扩展接口落地（双接口 + 契约 A）**——`providers.ts` 定义 `DayMetaProvider`（格子 meta，sub/badge 后注册覆盖前注册）/ `PanelProvider`（详情面板内容块，同步契约，异步自吞）；内置 provider（lunar/holiday/detail）默认注册、用户追加在后；`PanelProvider.render` 仅在选中日期变化时重渲（翻月不重渲）；`destroy()` 调各 provider `destroy?.()`。**0 字节承诺降级**：内置数据留包内默认注册（原"lunar 未引入时 0 字节"目标作废，README 口径改为"扩展不增体积"）。
- 2026-08-01：日历弹层外壳重构——**砍掉面板内 × 关闭按钮**（保留 Esc + 遮罩 + 底部取消；详情面板"关闭详情"× 保留）；取消/确认上提为**面板级 footer**（`panel.append(mainArea, detailPanel, actions)`，修复原 actions 重复 append 死代码）；打开时焦点移入「今天」按钮、关闭动画结束后归还输入框（最小档，无焦点陷阱）；**锚定动画**：transform-origin 由 JS 按输入框 rect 计算，打开 scale(0.92)→1、关闭回缩，`prefers-reduced-motion` 下 transition:none。calendar 包新增 vitest 测试基建（happy-dom，13 用例）。

## 四、开发进度与里程碑

- 2026-07-25：REFACTOR_PLAN.md v1.0 定稿。
- 2026-07-28：删除 plugin/calendar 旧 vendor 克隆；calendar 包骨架 + date 工具落地。
- 2026-07-31：calendar-core 并入 calendar（删除 core 包 + 清理 dayRenderer/RichDayMeta/selectedDetail 死代码，见"项目基础信息"）。
- 里程碑计划：W7 状态引擎可用 / W10 原生渲染可用 / W13 农历差异化 / W16 首发 0.1.0（详见 REFACTOR_PLAN.md §7）。
- 2026-07-28：新增 `.qw-input` 输入框组件（tokens 化 + @property 边框流光），落地于 demo 原型皮肤层。

## 五、踩坑记录与注意事项

- demo 页零外部请求、可直接 file:// 打开；农历/节气/节日数据为构建期预生成（覆盖 2023-10 ~ 2029-02），勿改为运行时请求。
- Biome CSS lint 已启用（demo 中已有 biome-ignore 注释先例），改样式后跑 `bun lint` 或 `bunx biome check <file>`。
- 动画需兼容 `prefers-reduced-motion`（demo base 层已有全局 kill switch，新增动画勿绕过）。

## 六、自定义扩展区

（暂空）
