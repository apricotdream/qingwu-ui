name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    name: Build / Typecheck / Test / Lint / Size
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run full pipeline
        run: bun run ci
